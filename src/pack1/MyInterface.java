package pack1;

public interface MyInterface {
	// by default all the data member in the interface are public static final
	int NUMBER =100;
	
	//by default all the method in the interface are public abstract
	void show(); 
	public abstract void display (String msg);
	

}
